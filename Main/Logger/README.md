# Система логирования

## Обзор архитектуры

Приложение использует централизованную систему логирования, построенную на следующих принципах:

```text
                           +------------------------+
                           |                        |
                           |  Logger/mainLogger.js  |
                           |  (Main Process)        |
                           |                        |
                           +------------------------+
                                      ↑
                                      |
            +------------------------+|+------------------------+
            |                        ||                        |
            |     IPC-канал          ||      Файловая система  |
            |  (log-message)         ||   (Запись логов)       |
            |                        ||                        |
            +------------------------+|+------------------------+
                                      |
                       +-------------+v+-------------+
                       |                             |
                       |      preload.mjs            |
                       |  (Preload Process)          |
                       |                             |
                       +-------------+--------------+
                                     |
                                     v
    +---------------------------+    |    +---------------------------+
    |                           |    |    |                           |
    |  electron.logger          |<---+    |  loggerService.js         |
    |  (window.electron.logger) |         |  (Renderer Process)       |
    |                           |         |                           |
    +------------+--------------+         +-------------+-------------+
                 |                                      |
                 v                                      v
    +---------------------------+         +---------------------------+
    |                           |         |                           |
    |  Прямое использование     |         |  Модули рендерера         |
    |  в рендерер-процессах     |         |  (через createLogger)     |
    |                           |         |                           |
    +---------------------------+         +---------------------------+
```

## Структура модулей логирования

Система логирования разделена на следующие модули:

- **index.js** - Центральный экспорт системы логирования
- **mainLogger.js** - Основной класс логгера
- **config.js** - Конфигурация и константы
- **formatters.js** - Форматирование логов для разных источников
- **utils.js** - Вспомогательные функции
- **fileLogger.js** - Работа с файлами логов
- **ipcLogger.js** - Логирование IPC сообщений
- **exceptionHandler.js** - Обработка исключений

## Компоненты системы логирования

### 1. Main Process (Logger/mainLogger.js)

Центральный логгер, работающий в main-процессе, отвечает за:

- Прием логов от всех процессов через IPC-канал
- Запись логов в файлы
- Ротацию и управление файлами логов
- Вывод форматированных логов в консоль

### 2. Preload Process (preload.mjs)

Мост между renderer и main процессами:

- Экспортирует API логирования через contextBridge
- Предоставляет функцию создания логгеров для renderer-процессов
- Передает сообщения логов в main-процесс через IPC

### 3. Renderer Process (loggerService.js)

Интерфейс логирования для рендерер-процессов:

- Предоставляет унифицированный интерфейс через функцию `createLogger`
- Использует API из preload для передачи логов в main-процесс
- Включает fallback для случаев, когда основной логгер недоступен

## Уровни логирования

Система поддерживает следующие уровни логирования:

| Уровень | Описание | Пример использования |
|---------|----------|----------------------|
| debug   | Детальная отладочная информация | Значения переменных, промежуточные результаты |
| info    | Информационные сообщения | Успешные операции, инициализация модулей |
| warn    | Предупреждения | Некритичные ошибки, незначительные проблемы |
| error   | Критические ошибки | Исключения, сбои в работе приложения |

Дополнительные специальные методы:

- `success` - для логирования успешных операций (уровень info с эмодзи ✅)
- `ready` - для логирования загрузки модулей (уровень info)
- `directory` - для логирования работы с директориями (уровень info)

## Использование в Main-процессе

```javascript
import { mainLogger } from '@Main/Logger';

// Основные методы логирования
mainLogger.debug('Main/модуль', 'Отладочное сообщение');
mainLogger.info('Main/модуль', 'Информационное сообщение');
mainLogger.warn('Main/модуль', 'Предупреждение');
mainLogger.error('Main/модуль', 'Ошибка', { дополнительные: 'данные' });

// Специальные методы
mainLogger.ready('Main/модуль'); // Логирует "module loaded"
mainLogger.success('Main/модуль', 'Операция успешно выполнена');
mainLogger.directory('Имя директории'); // Логирует "📂 Имя директории loaded"
```

## Использование в Renderer-процессе

### Рекомендованный способ (через loggerService.js)

```javascript
import { createLogger } from '@FileManager/Utilities/loggerService.js';

// Создание именованного логгера для модуля
const logger = createLogger('UI/модуль');

// Использование методов логгера
logger.debug('Отладочное сообщение', { дополнительные: 'данные' });
logger.info('Информационное сообщение');
logger.warn('Предупреждение');
logger.error('Ошибка', error, { контекст: 'дополнительный' });
logger.success('Операция успешно выполнена');
logger.logObject(объект, 'Дамп объекта');
```

### Альтернативный способ (прямое использование)

```javascript
// Прямая отправка лога в main-процесс
window.electron.logger.log('info', 'UI/модуль', 'Сообщение', { данные: 'объект' });

// Создание логгера напрямую
const logger = window.electron.logger.createLogger('UI/модуль');
logger.info('Сообщение');
```

## Конфигурация логгера

Основные параметры конфигурации логгера находятся в файле `Main/Logger/config.js`:

```javascript
// Настройки по умолчанию
const DEFAULT_CONFIG = {
  // Пути к файлам
  logDirectory: path.join(app.getPath('userData'), 'logs'),
  mainLogFile: 'app.log',
  ipcLogFile: 'ipc.log',
  sessionLogFile: 'session.log',
  
  // Файлы логов
  legacyLogFile: 'debug.log',
  useLegacyLogFile: true,
  
  // Структурированный JSON-лог
  aiLogFile: 'ai_debug.jsonl',
  useAiLogFile: true,
  
  // Настройки ротации
  maxLogSize: 10 * 1024 * 1024, // 10 MB
  maxLogFiles: 5,
  
  // Настройки логирования
  logToConsole: true,
  logToFile: true,
  logToIpcFile: false,
  minLogLevel: 'info',
  consoleLogLevel: 'info',
  
  // Форматирование
  colorize: true,
  includeTimestamp: true,
  
  // Интеграция
  captureIPC: true,
  captureUncaughtExceptions: true
};
```

## Просмотр логов

Логи сохраняются в следующих файлах:

- `app.log` - основной файл логов
- `session.log` - логи текущей сессии
- `debug.log` - стандартный файл логов в корне проекта
- `ai_debug.jsonl` - структурированные логи в формате JSON

## Дополнительные возможности

- **Получение логов для UI**: `window.electron.logger.getLogs(options)`
- **Получение конфигурации**: `window.electron.logger.getConfig()` 