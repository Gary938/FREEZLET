# Стандарт рефакторинга файлов под AI-Rules
## Методология поэтапной оптимизации проблемных модулей

### 🎯 Принцип
**СНАЧАЛА оптимизируем существующий файл, ПОТОМ разбиваем его на модули**

Цель: превратить проблемный файл в AI-совместимый код за минимальные изменения

---

## 📋 Обязательные этапы рефакторинга

### **ЭТАП 1: Упрощение и очистка файла**
*Убираем код-мусор и избыточность*

#### 1.1 Очистка логирования
- ✅ **Удалить избыточные trace вызовы** (оставить только критичные)
- ✅ **Объединить похожие trace** в один вызов
- ✅ **Убрать детальные trace из простых операций** (поиск элементов, присвоения)
- ✅ **Оставить trace только для**: ошибок, важных переходов состояния, результатов операций

```javascript
// ❌ ИЗБЫТОЧНО
tracer.trace('attachHandler:start', { hasHandler: !!handlerFunction });
tracer.trace('attachHandler:buttonSearch', { buttonFound: !!nextBlockBtn });
tracer.trace('attachHandler:success', { buttonId: nextBlockBtn.id });

// ✅ ОПТИМАЛЬНО  
tracer.trace('attachHandler', { 
    success: !!nextBlockBtn,
    handlerAttached: !!handlerFunction 
});
```

#### 1.2 Упрощение кода
- ✅ **Убрать дублированные проверки** и условия
- ✅ **Объединить похожие операции** в циклы или утилиты
- ✅ **Убрать неиспользуемые переменные** и временные значения
- ✅ **Упростить вложенные условия** через guard clauses
- ✅ **Убрать избыточные комментарии** и пустые строки

#### 1.3 Оптимизация функций
- ✅ **Разбить функции >18 строк** на более мелкие
- ✅ **Извлечь повторяющуюся логику** в helper функции
- ✅ **Убрать глубокую вложенность** (>3 уровней)

#### 1.4 🔥 **КРИТИЧНО: Удаление инлайн стилей**
- ✅ **Найти все инлайн стили** в HTML строках
- ✅ **Проверить существование CSS файлов** с аналогичными стилями
- ✅ **Удалить дублированные инлайн стили** если они есть в CSS
- ✅ **Перенести уникальные стили** в соответствующие CSS файлы
- ✅ **Использовать CSS классы** вместо style атрибутов

```javascript
// ❌ НЕПРАВИЛЬНО - инлайн стили в JS
const html = `<button style="position: absolute; color: #4caf50;">Click</button>`;

// ✅ ПРАВИЛЬНО - CSS классы
const html = `<button class="next-block-button">Click</button>`;
// И в CSS файле: .next-block-button { position: absolute; color: #4caf50; }
```

**Принцип разделения ответственностей:**
- **HTML** → структура элементов
- **CSS** → внешний вид и стили  
- **JavaScript** → логика и поведение

### **ЭТАП 2: Анализ секций модуля**
*Проверяем необходимость каждой секции*

#### 2.1 Анализ секции CONFIG
- ❓ **Содержит ли реальные константы/лимиты/правила?**
- ❓ **Используются ли эти константы в коде?**
- ❓ **Нет ли дублирования с другими CONFIG?**

```javascript
// ✅ НУЖНЫЙ CONFIG - реальные константы
export const TRANSITIONS_CONFIG = {
    NEXT_BLOCK_ID: 'nextBlock',
    VIDEO_SRC: 'CSS/Content/LernMod/Video/NextBlock.mp4',
    AUTO_ADVANCE_DELAY: 1600
};

// ❌ НЕНУЖНЫЙ CONFIG - искусственные константы
export const CONFIG = {
    MODULE_NAME: 'transitions' // не используется
};
```

#### 2.2 Анализ секции TYPES
- ❓ **Определяет ли модуль собственные типы/enum?**
- ❓ **Или импортирует типы из других модулей?**
- ✅ **Убрать секцию TYPES если импортируются извне**

#### 2.3 Анализ секции HELPERS
- ❓ **Содержит ли переиспользуемые приватные функции?**
- ❓ **Или только однострочные утилиты?**
- ✅ **Убрать HELPERS если функции используются только один раз**

### **ЭТАП 3: Проверка использования функций**
*Убираем мертвый код*

#### 3.1 Поиск неиспользуемых экспортов
```bash
# Команды для проверки использования функций
grep -r "имя_функции" src/ --exclude-dir=node_modules
```

#### 3.2 Анализ каждого экспорта
- ✅ **Ищем импорты функции** в других файлах
- ✅ **Ищем прямые вызовы** функции
- ✅ **Проверяем использование в тестах**
- ❌ **Удаляем неиспользуемые экспорты**

#### 3.3 Рефакторинг зависимостей
- ✅ **Заменяем неиспользуемые сложные функции** на простые
- ✅ **Объединяем функции с одинаковой логикой**

### **ЭТАП 4: Проверка принадлежности слою**
*Проверяем архитектурную корректность*

#### 4.1 Определение текущего слоя
```
UI/LearnMode-Hybrid/
├── Components/     - UI компоненты, рендеринг, DOM
├── Core/          - Бизнес-логика, состояние
├── Controllers/   - Координация, маршрутизация
├── Utils/         - Утилиты, helper функции
├── Config/        - Конфигурация, константы
└── Facade/        - Внешний API, изоляция
```

#### 4.2 Анализ функций по принадлежности
```javascript
// ✅ ПРАВИЛЬНО - Components/Screen/
export const createBlockTransition = () => { /* DOM создание */ };
export const renderBlockTransition = () => { /* DOM рендеринг */ };

// ❌ НЕПРАВИЛЬНО - Components содержит бизнес-логику
export const calculateNextStage = () => { /* должно быть в Core/ */ };

// ❌ НЕПРАВИЛЬНО - Components содержит координацию
export const coordianteUserFlow = () => { /* должно быть в Controllers/ */ };
```

#### 4.3 Перемещение функций
- ✅ **Бизнес-логику** → `Core/`
- ✅ **Координацию** → `Controllers/`  
- ✅ **Утилиты** → `Utils/`
- ✅ **Конфигурацию** → `Config/`
- ✅ **DOM операции** → `Components/`

### **ЭТАП 5: Разбиение на модули (если нужно)**
*Применяется ТОЛЬКО если файл все еще >120 строк*

#### 5.1 Создание структуры папки
```
Components/Screen/Transitions/     # Папка = название исходного файла
├── transitionRenderer.js         # Модуль 1: рендеринг и DOM
├── transitionHandlers.js         # Модуль 2: обработка событий  
├── transitionVideo.js            # Модуль 3: управление видео
├── transitionLogic.js            # Модуль 4: логика переходов
└── index.js                      # Централизованный экспорт
```

#### 5.2 Принципы разделения модулей

**По единой ответственности:**
- **Renderer модуль**: создание DOM, HTML генерация, рендеринг
- **Handlers модуль**: обработка событий, callback функции
- **Business модуль**: бизнес-логика, вычисления, алгоритмы
- **Utils модуль**: вспомогательные функции, утилиты

**Размеры модулей:**
- ✅ **20-120 строк** каждый модуль
- ✅ **8-18 строк** каждая функция
- ✅ **Не более 5-7 функций** в модуле

#### 5.3 Создание index.js
```javascript
// Components/Screen/Transitions/index.js
export { 
    createBlockTransition,
    renderBlockTransition 
} from './transitionRenderer.js';

export { 
    attachNextBlockHandler,
    removeNextBlockHandler 
} from './transitionHandlers.js';

export { 
    playNextBlockVideo 
} from './transitionVideo.js';

export { 
    createTransitionComponent 
} from './transitionLogic.js';
```

#### 5.4 Обновление импортов
- ✅ **Заменить импорты** в других файлах
- ✅ **Использовать index.js** как entry point
- ✅ **Проверить работоспособность** webpack сборки

---

## 📊 Чеклист выполнения этапов

### ✅ ЭТАП 1: Упрощение завершен если:
- [ ] Trace вызовов стало в 2-3 раза меньше
- [ ] Функции не превышают 18 строк  
- [ ] Нет вложенности >3 уровней
- [ ] Убраны дублированные проверки
- [ ] **Все инлайн стили удалены/перенесены в CSS**
- [ ] Код стал читаемее

### ✅ ЭТАП 2: Анализ секций завершен если:
- [ ] CONFIG содержит только реальные константы
- [ ] TYPES содержит только собственные типы модуля
- [ ] HELPERS содержит только переиспользуемые функции
- [ ] Ненужные секции удалены

### ✅ ЭТАП 3: Проверка использования завершена если:
- [ ] Все экспорты имеют импорты в других файлах
- [ ] Мертвый код удален
- [ ] Дублированные функции объединены

### ✅ ЭТАП 4: Проверка слоя завершена если:
- [ ] Все функции соответствуют слою модуля
- [ ] Неправильные функции перемещены в другие слои
- [ ] Архитектура стала корректной

### ✅ ЭТАП 5: Разбиение завершено если:
- [ ] Все новые модули 20-120 строк
- [ ] Каждый модуль имеет единую ответственность
- [ ] index.js создан с централизованными экспортами
- [ ] Все импорты обновлены
- [ ] Webpack собирается успешно

---

## 🎯 Ожидаемые результаты

### После ЭТАПОВ 1-4 (оптимизация):
- **Размер файла**: сокращение на 30-50%
- **Читаемость**: улучшение на 70-80%
- **Время понимания ИИ**: снижение с 15+ до 8-12 секунд
- **Архитектурная чистота**: разделение HTML/CSS/JS

### После ЭТАПА 5 (разбиение):
- **Размер модулей**: 100% соответствие лимиту 20-120 строк
- **Время понимания ИИ**: 6-8 секунд на модуль
- **Архитектурная чистота**: четкое разделение ответственностей

---

## ⚠️ Важные правила

1. **НИКОГДА не разбивать файл** без прохождения этапов 1-4
2. **ВСЕГДА начинать с упрощения** существующего кода
3. **ОБЯЗАТЕЛЬНО проверять использование** всех функций
4. **ВСЕГДА проверять принадлежность слою** перед разбиением
5. **🔥 КРИТИЧНО: Убирать инлайн стили** в пользу CSS классов
6. **ЗАПРЕЩЕНО создавать модули <20 строк** (объединять в более крупные)
7. **ОБЯЗАТЕЛЬНО тестировать webpack** после каждого этапа

---

## 📋 Правила создания документации

### ⚠️ **ВАЖНО: Отчеты рефакторинга**
- **НЕ создавать документы рефакторинга автоматически**
- **Создавать отчет ТОЛЬКО если пользователь явно попросил**
- **Фокус на коде, а не на документации**

### 📝 Шаблон отчета по этапам (использовать только по запросу)

```markdown
## Рефакторинг [ФАЙЛ_NAME].js

### ЭТАП 1: Упрощение
- Убрано trace вызовов: XX → YY (-ZZ%)
- Сокращено функций >18 строк: XX → YY  
- Убрано строк кода: XXX → YYY (-ZZ%)
- **Удалено инлайн стилей: XX (перенесено в CSS)**

### ЭТАП 2: Анализ секций
- CONFIG: упрощен/удален
- TYPES: оставлен/удален  
- HELPERS: упрощен/удален

### ЭТАП 3: Проверка использования
- Удалено неиспользуемых экспортов: XX
- Объединено дублированных функций: XX

### ЭТАП 4: Проверка слоя
- Перемещено функций в другие слои: XX
- Архитектура соответствует: ✅/❌

### ЭТАП 5: Результат
- Размер после оптимизации: XXX строк
- Требуется разбиение: ✅/❌
- Создано модулей: XX
```

---

**Этот стандарт обязателен для всех случаев рефакторинга файлов превышающих AI-Rules лимиты.** 