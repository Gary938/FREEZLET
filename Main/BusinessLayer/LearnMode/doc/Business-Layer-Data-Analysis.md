# АНАЛИЗ ДАННЫХ БИЗНЕС-СЛОЯ LEARNMODE
## Исчерпывающий справочник форматов данных для UI интеграции

---

## 📊 ОБЗОР АРХИТЕКТУРЫ ДАННЫХ

**Проанализировано:** 25+ файлов бизнес-слоя LearnMode  
**Типов ответов:** 4 основных + расширения  
**Форматов данных:** 8 структур данных  
**Extensions:** 2 интерактивных компонента (Pacman, Background)

---

## 🔄 ОСНОВНЫЕ ТИПЫ ОТВЕТОВ

### 📝 **1. QUESTION (Вопрос)**
**Файл источник:** `Core/ResponseBuilder/questionBuilder.js`  
**Частота:** При каждом новом вопросе

```javascript
// Базовая структура
{
    type: "question",
    success: true,
    data: {
        id: "q_12345",                    // Уникальный ID вопроса
        question: "What is...?",           // Текст вопроса
        options: ["option1", "option2"],   // Варианты ответов (перемешанные)
        correctAnswer: 1,                  // Индекс правильного ответа (после перемешивания)
        
        // Пример для показа после ответа (ПРАВИЛЬНАЯ СТРУКТУРА)
        showExample: {
            text: "Example text...",       // Текст примера
            position: "after_answers"      // Позиция показа
        },
        
        // Extensions перемещены в data секцию (ИСПРАВЛЕНО)
        pacman: {                         // Данные Pacman (только при инициализации)
            action: "init",               // Команда для Pacman
            totalQuestions: 20,           // Общее количество вопросов
            totalGhosts: 20,              // Призраков на текущей странице
            currentPosition: 0,           // Текущая позиция Pacman
            
            // Данные пагинации (для больших тестов >30 вопросов)
            pagination: {
                totalPages: 1,            // Общее количество страниц
                ghostsPerPage: 20,        // Призраков на странице
                pages: [{                 // Информация о страницах
                    pageIndex: 0,
                    startGhost: 0,
                    endGhost: 20,
                    ghostCount: 20
                }],
                needsPagination: false    // Нужна ли пагинация
            },
            
            // Метаданные
            lastAction: "init",
            isAnimating: false,
            createdTime: 1704067200000,
            updateTime: 1704067200000
        },
        
        // Background данные (если изменился)
        background: {
            currentPath: "CSS/Content/LernMod/Stories/Story1/slide_1.jpg",
            mode: "story",                // "story" | "random"
            reason: "test_start"          // Причина смены (опционально)
        }
    },
    timestamp: 1704067200000
}
```

**Когда отправляется:**
- При старте теста (`startTest()`)
- После обработки ответа (если есть следующий вопрос)
- При переходе между вопросами в блоке

---

### 🏁 **2. NEXT_BLOCK (Переход между блоками)**
**Файл источник:** `Core/ResponseBuilder/blockBuilder.js`  
**Частота:** При завершении блока (каждые 5-20 вопросов)

```javascript
{
    type: "next_block",
    success: true,
    data: {
        blockNumber: 2,                    // Номер нового блока
        message: "Блок завершен! Переход к блоку 2",
        totalBlocks: 4,                    // Общее количество блоков
        perfectBlock: true,                // Был ли блок пройден идеально
        nextQuestion: {                    // Первый вопрос нового блока
            id: "q_67890",
            question: "Next question text...",
            options: ["A", "B", "C", "D"],
            correctAnswer: 2
        },
        
        // Background данные перемещены в data секцию (ИСПРАВЛЕНО)
        background: {                      // Только при perfectBlock: true
            currentPath: "CSS/Content/LernMod/Stories/Story1/slide_2.jpg",
            mode: "story",
            reason: "perfect_block"
        }
    },
    timestamp: 1704067260000
}
```

**Когда отправляется:**
- При завершении блока (все вопросы блока отвечены)
- Содержит данные следующего блока и первый вопрос

---

### 🎯 **3. FINAL_STATS (Финальные результаты)**
**Файл источник:** `Operations/ResultCalculation/resultFormatter.js`  
**Частота:** При завершении всего теста

```javascript
{
    type: "final_stats",
    success: true,
    data: {
        // Основные результаты
        testId: "test_uuid_12345",
        testPath: "Tests/example/test.txt",
        stage: 0,                          // Текущая стадия
        currentStage: 0,                   // Текущая стадия (дубликат для совместимости)
        nextStage: 1,                      // Следующая стадия (если прошел)
        
        // Статистика точности
        accuracy: 85.5,                    // Точность в процентах (0-100)
        correctCount: 17,                  // Количество правильных ответов
        totalCount: 20,                    // Общее количество вопросов
        
        // Результат прохождения
        passed: true,                      // Прошел ли тест
        stars: 2,                          // Количество звезд (1-3)
        resultVideo: "90%.mp4",            // Видео для показа
        
        // Метаданные
        elapsedTimeSeconds: 180,           // Время прохождения в секундах
        blocks: 4,                         // Количество блоков
        wasIdealLastBlock: true,           // Был ли последний блок идеальным
        completedAt: "2024-01-01T12:00:00.000Z",
        
        // Background данные перемещены в data секцию (ИСПРАВЛЕНО)
        background: {                      // Только при wasIdealLastBlock: true
            currentPath: "CSS/Content/LernMod/Stories/Story2/slide_1.jpg",
            mode: "story",
            reason: "test_completion"
        }
    },
    timestamp: 1704067320000
}
```

**Когда отправляется:**
- При завершении всего теста (все блоки пройдены)
- Содержит полную статистику и прогресс

---

### ❌ **4. ERROR (Ошибка)**
**Файл источник:** `Config/responseTypes.js`, `Core/errorHandler.js`  
**Частота:** При возникновении ошибок

```javascript
{
    type: "error",
    data: {
        code: "FILE_NOT_FOUND",            // Код ошибки
        message: "Файл теста не найден",   // Человекочитаемое сообщение
        details: {                         // Дополнительные детали
            testPath: "invalid/path.txt",
            operation: "test initialization"
        }
    },
    timestamp: 1704067380000
}
```

**Коды ошибок:**
- `NO_ACTIVE_TEST` - Нет активного теста
- `INVALID_RESULT` - Неверный результат ответа
- `FILE_NOT_FOUND` - Файл теста не найден
- `PARSE_ERROR` - Ошибка парсинга теста
- `OPERATION_FAILED` - Общая ошибка операции

---

## 🎮 EXTENSIONS (РАСШИРЕНИЯ)

### 🟡 **PACMAN EXTENSION**
**Файл источник:** `Interactive/Pacman/commandBuilder.js`  
**Когда добавляется:** Только при инициализации теста

```javascript
data: {
    pacman: {
        action: "init",                    // Команда для Pacman
        totalQuestions: 20,                // Общее количество вопросов
        
        // Данные пагинации (для больших тестов >30 вопросов)
        pagination: {
            totalPages: 1,                 // Общее количество страниц
            ghostsPerPage: 20,             // Призраков на странице
            pages: [{                      // Информация о страницах
                pageIndex: 0,
                startGhost: 0,
                endGhost: 20,
                ghostCount: 20
            }],
            needsPagination: false         // Нужна ли пагинация
        },
        
        // Текущее состояние
        currentPage: 0,                    // Текущая страница
        currentPageData: {                 // Данные текущей страницы
            pageIndex: 0,
            startGhost: 0,
            endGhost: 20,
            ghostCount: 20
        },
        totalGhosts: 20,                   // Призраков на текущей странице
        currentPosition: 0,                // Текущая позиция Pacman
        
        // Метаданные
        lastAction: "init",
        isAnimating: false,
        createdTime: 1704067200000,
        updateTime: 1704067200000
    }
}
```

**Особенности:**
- Отправляется только при `startTest()`
- Содержит полную информацию о пагинации
- UI использует `totalGhosts` для отрисовки призраков

---

### 🖼️ **BACKGROUND EXTENSION**
**Файл источник:** `Interactive/Background/modeManager.js`  
**Когда добавляется:** При старте теста + при срабатывании триггеров

```javascript
data: {
    background: {
        currentPath: "CSS/Content/LernMod/Stories/Story1/slide_3.jpg",
        mode: "story",                     // "story" | "random"
        reason: "perfect_block"            // Опционально: причина смены
    }
}
```

**Режимы:**
- `"story"` - Последовательные слайды из историй
- `"random"` - Случайные изображения

**Триггеры смены фона:**
1. **При старте теста** - загружается текущий фон из БД
2. **При идеальном блоке** - переход к следующему слайду истории
3. **При завершении теста** - переход к следующему слайду (если последний блок идеален)

**Значения `reason`:**
- `"perfect_block"` - смена при идеальном прохождении блока
- `"test_completion"` - смена при завершении теста

---

## 📋 ДЕТАЛЬНАЯ СТРУКТУРА ДАННЫХ

### 🔤 **QUESTION DATA STRUCTURE**
```javascript
// Исходная структура из бизнес-слоя
{
    id: string,                            // Уникальный идентификатор
    question: string,                      // Текст вопроса
    options: string[],                     // Массив вариантов ответов (перемешанный)
    correctAnswer: number,                 // Индекс правильного ответа (0-based)
    example?: string                       // Опциональный пример
}
```

**Обработка в бизнес-слое:**
1. Исходные данные загружаются из файла теста
2. `shuffleOptions()` перемешивает варианты ответов
3. `correctAnswer` обновляется после перемешивания
4. `getPendingExample()` добавляет пример если есть

---

### 📊 **STATS CALCULATION**
```javascript
// Внутренняя структура расчета статистики
{
    accuracy: number,                      // Точность в процентах (0-100)
    correctAnswers: number,                // Количество правильных ответов
    totalQuestions: number,                // Общее количество вопросов
    testDuration: number,                  // Длительность в миллисекундах
    perfectBlocks: number                  // Количество идеальных блоков
}
```

---

### 🏆 **STAGE PROGRESSION**
```javascript
// Правила прогрессии по стадиям (Config/stageConfig.js)
{
    stage: 0,                              // Текущая стадия (0-3)
    accuracyThreshold: 0.8,                // Минимальная точность для прохождения
    blockSize: 5,                          // Размер блока на данной стадии
    starsRules: {                          // Правила начисления звезд
        1: { minAccuracy: 0.8 },           // 1 звезда при 80%+
        2: { minAccuracy: 0.8 },           // 2 звезды при 80%+ на стадии 1
        3: { minAccuracy: 1.0 }            // 3 звезды при 100% на стадии 3
    }
}
```

---

## 🔄 ПОТОКИ ДАННЫХ

### 🚀 **1. ИНИЦИАЛИЗАЦИЯ ТЕСТА**
```
UI запрос: startTest(testPath, stage?)
         ↓
Business Layer: 
├── Загрузка файла теста
├── Создание состояния теста
├── Инициализация Pacman (extensions.pacman)
├── Загрузка фона (extensions.background)
         ↓
UI ответ: QUESTION + extensions
```

### ✅ **2. ОБРАБОТКА ОТВЕТА**
```
UI запрос: submitAnswer("correct"|"incorrect")
         ↓
Business Layer:
├── Обновление статистики
├── Проверка завершения блока
├── Проверка завершения теста
         ↓
UI ответ: QUESTION | NEXT_BLOCK | FINAL_STATS
```

### 🎯 **3. ЗАВЕРШЕНИЕ ТЕСТА**
```
Business Layer:
├── Расчет финальных результатов
├── Сохранение прогресса в БД
├── Очистка состояния
├── Триггер смены фона (если идеальный блок)
         ↓
UI ответ: FINAL_STATS + extensions.background?
```

---

## 🎮 ИНТЕРАКТИВНЫЕ КОМПОНЕНТЫ

### 🟡 **PACMAN INTEGRATION**
**Инициализация:**
- Отправляется при `startTest()`
- Содержит `totalGhosts` = количество вопросов на первой странице
- Поддерживает пагинацию для тестов >30 вопросов

**Движение:**
- Бизнес-слой НЕ отправляет команды движения
- UI самостоятельно управляет движением при правильных ответах

### 🖼️ **BACKGROUND INTEGRATION**
**Auto-loading:**
- При старте теста всегда загружается текущий фон
- Формат: `{ currentPath: "путь", mode: "story|random" }`

**Dynamic Updates:**
- При идеальном прохождении блока
- При завершении теста с идеальным последним блоком
- Автоматический переход к следующему слайду истории

---

## ⚠️ ВАЖНЫЕ ОСОБЕННОСТИ

### 🔒 **ВАЛИДАЦИЯ ДАННЫХ**
- Все ответы проходят валидацию в `Core/validator.js`
- Недопустимые данные возвращают ERROR response
- Проверка состояния теста перед каждой операцией

### 🔄 **СОСТОЯНИЕ ТЕСТА**
- Состояние сохраняется в `Core/State/stateStore.js`
- Автоматическая очистка при завершении теста
- Поддержка одновременно только одного активного теста

### 📦 **EXTENSIONS HANDLING**
- Extensions добавляются условно (только при наличии данных)
- Pacman: только при инициализации
- Background: при старте + при триггерах смены

### 🎯 **СОВМЕСТИМОСТЬ С UI**
- Все форматы протестированы с UI LearnMode-Hybrid
- Поддержка legacy полей для обратной совместимости
- Timestamp в каждом ответе для отладки

---

## 📝 ЗАКЛЮЧЕНИЕ

Бизнес-слой LearnMode отправляет в UI **4 основных типа данных** с **2 типами расширений**. Архитектура данных оптимизирована для:

✅ **Производительности** - минимальные данные в каждом ответе  
✅ **Расширяемости** - система extensions для новых функций  
✅ **Отладки** - timestamp и детальная информация об ошибках  
✅ **Совместимости** - поддержка различных UI реализаций

**Общий объем данных:** ~2-5KB на ответ (зависит от размера вопроса и extensions)

---

## 🔄 СИНХРОНИЗАЦИЯ ЗАВЕРШЕНА

**Дата обновления:** 2024-12-29  
**Статус:** ✅ СИНХРОНИЗИРОВАНО с реальной реализацией

### Исправленные рассогласования:
1. **Extensions размещение**: Перемещены из `extensions` в `data` секцию
2. **Структура showExample**: Исправлено с простой строки на объект с полями `text` и `position`
3. **Актуализация PACMAN данных**: Обновлена структура согласно реальной реализации
4. **Консистентность с UI-Business-Data-Interface-Input.md**: Документы полностью синхронизированы

Теперь документация точно отражает реальную архитектуру данных между бизнес-слоем и UI.